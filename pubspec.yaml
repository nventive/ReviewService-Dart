name: review_service
description: "A multi-platform Dart package to prompt users for app reviews, with logging support."
version: 0.9.9 # The version will be overriden in the CI following semantic versioning.
homepage: https://github.com/nventive/ReviewService-Dart
repository: https://github.com/nventive/ReviewService-Dart

environment:
  sdk: "^3.3.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  in_app_review: ^2.0.9
  logger: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^3.0.0
  mockito: ^5.4.4
  build_runner: ^2.4.8
